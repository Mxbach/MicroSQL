# MicroSQL
A lightweight relational database engine built from scratch in C++. Features SQL parsing, B-tree indexes, transaction support, and query optimization. Educational implementation demonstrating database internals.
